spring:
  application:
    name: hoge-stock-keeper-api
  profiles:
    default: local

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
