spring:
  application:
    name: hoge-customer-registry-api
  profiles:
    default: local

logging:
  level:
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
